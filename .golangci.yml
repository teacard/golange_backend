run:
  # 指定 golangci-lint 分析時使用的 Go 語言版本目標
  # 設為 1.24 是因為 golangci-lint 1.64.x 本身以 Go 1.24 編譯，
  # 若目標版本高於編譯版本（如 go.mod 的 go 1.25）會直接拒絕執行。
  # 此設定讓 lint 以 1.24 標準分析，實際編譯仍使用 go.mod 指定的版本。
  go: "1.24"
